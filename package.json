{
  "name": "repobridge",
  "version": "0.1.0",
  "description": "Fetch source code for packages and repositories for agent context",
  "type": "module",
  "bin": {
    "repobridge": "./bin/repobridge.js"
  },
  "files": [
    "bin",
    "scripts",
    "README.md"
  ],
  "scripts": {
    "build": "node scripts/build-all.js",
    "build:all": "node scripts/build-all.js",
    "build:native": "go build -ldflags \"-X main.version=$npm_package_version\" -o bin/repobridge ./cmd/repobridge && node scripts/copy-native.js",
    "test": "go test ./...",
    "lint": "go vet ./...",
    "format": "gofmt -w ./cmd ./internal"
  },
  "keywords": [
    "cli",
    "source-code",
    "ai",
    "coding-agents"
  ],
  "license": "Apache-2.0"
}
